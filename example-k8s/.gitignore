node_modules
dist
# chant build artifact — regenerated by `chant build` / `chant run k3d-apply`
app.yaml
ops/*/
# bring-up/down state (scripts/local/local-up.sh saves the pre-demo kubectl
# context here so local-down.sh can restore it)
scripts/local/.prev-context
