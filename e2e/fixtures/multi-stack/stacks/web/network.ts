// Placeholder source for the "web" stack (e2e/fixtures/multi-stack, #76).
// Empty on purpose — this fixture backs `graphPath`'s stack-src resolution,
// not a real chant build; a distinct file per stack is enough to make the
// two stacks' source trees observably different on disk.
export {};
