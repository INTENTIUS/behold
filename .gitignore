node_modules
dist
*.log
.DS_Store

# The estate examples' member apps are installed by the demos/e2es; their
# locks are churn, deliberately untracked (the estates' top-level locks and
# example-carve/app's stay tracked).
example-argo-estate/*/package-lock.json
example-flux-estate/*/package-lock.json

# UI smoke screenshots (#199)
smoke/shots/

# Demos loaded into this checkout by `behold demo` / the panel's catalog (#268)
behold-demo/
behold-demos/

# Claude Code agent worktrees / local state
.claude/

# Astro build cache
docs/.astro/
